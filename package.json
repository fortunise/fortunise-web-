{
  "name": "fortunise-vercel-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve public",
    "build": "mkdir public && cp -r build/v4/* public/"
  },
  "dependencies": {
    "@vercel/analytics": "^1.5.0",
    "serve": "^14.2.5"
  }
}
